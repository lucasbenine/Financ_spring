import react from 'react';
import { useState } from 'react';
import api from '../api';
import styled from 'styled-components';

const MovimentacaoWrapper = styled.div`
    width: 100vw;
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
`;

function Movimentacao() {
    return(
        <MovimentacaoWrapper>

        </MovimentacaoWrapper>
    )
}

export default Movimentacao;